# kneecap — honest milestone status

**As of:** 2026-08-18 (post plan-execution campaign). Plan: `~/.claude/plans/opencut-mobile-port.md` (ratified 2026-08-17). Evidence rule: a milestone is only "done" if the claimed behavior was actually run and observed; everything else says so.

| Milestone | Status | Evidence / caveats |
|---|---|---|
| **M0** Fork, strip network, hygiene, invariants gate | **Done** | Freesound/remote-fonts/auth/Postgres path removed; `scripts/offline-audit.mjs` passes and was proven to catch an injected violation; `scripts/invariants.sh` is the merge gate and is **green** on this commit; CI workflow runs it; README/NOTICE/THIRD_PARTY_NOTICES/DECISIONS.md in place. |
| **M1** Feasibility spike | **Harness built; spike NOT run (device-blocked)** | Hidden diagnostics screen (`apps/mobile` `#/diagnostics` → spike.html) with the 6 tests + JSON export; `docs/SPIKE-GUIDE.md` has install steps and the kill-thresholds table. Tests 4 (whisper) and 5 (OPFS 200MB) verified locally; tests 1/6 rely on `requestAnimationFrame` and could not complete under background-tab automation (expected artifact, see SPIKE-GUIDE). **The actual go/no-go numbers require the founder's real devices.** |
| **M2** Headless editor-core + frozen EDL v1 | **Done** | `packages/editor-core` standalone-typechecks with 0 errors; headless gate scans 400 files for UI/server imports (0); EDL v1 (`docs/EDL.md`, integer ticks + rational fps) frozen, round-trip + golden-fixture tests green. |
| **M3** Capacitor shells + NativeBridge seam | **Done** | Both native projects build (iOS simulator `CODE_SIGNING_ALLOWED=NO`: BUILD SUCCEEDED; Android `assembleDebug`: BUILD SUCCESSFUL); bridge-import gate (no shell SDK imports outside `packages/native-bridge`) enforced in invariants, 0 hits. |
| **M4** Native media custody + proxy pipeline | **Built, compile-verified; runtime partially verified** | iOS: PHPicker → container copy → proxy transcode → thumbnails, exercised in simulator with a bundled CC0 clip. Android: same pipeline via Photo Picker + Media3 Transformer, **JVM unit tests only — instrumented tests never ran** (no emulator system images on the build machine; AVD exists but is broken). |
| **M5** Touch input layer | **Done** | Timeline controllers rewritten to pointer events; mouse-event gate is strict in invariants (0 raw mouse listeners); gestures exercised in the dev harness. |
| **M6** CapCut design tokens + component kit (B1) | **Done, with measurement debt** | Tokens measured from real App Store screenshots (`docs/capcut-reference/`); **~24 `[NEEDS-CAPTURE]` flags** across tokens.css/components mark values that could not be measured from available sources — the M6a founder capture session closes them (see FOUNDER-RUNBOOK). |
| **M7** Timeline component | **Built + wired; known gap** | Originally built against mock data (audit finding); fixer wired it to live EditorCore state via `use-timeline-project-vm.ts` and mounted it in `EditorShell`. `handleTrimCommit`'s own comment discloses trim-commit wiring still needs a follow-up pass. |
| **M8** Panels, toolbars, export sheet | **Done (post-audit fixes)** | Audit pressed every control against live engine state in a real browser. Fixed since: preview-canvas 0×0 collapse under an open sheet (two separate bugs), empty-Edit-sheet prompt, opacity/blendMode preview reflection, captions panel wiring. |
| **M9** Native export bridges | **Built; iOS partially verified, Android device-blocked** | Merge unified two incompatible wire contracts (see the MERGE NOTE atop `packages/native-bridge/src/capacitor-bridge.ts` before touching the bridge). iOS: EDL→AVFoundation mapper compiles, golden-frame harness ran in the simulator and produced a score. Android: EDL→Media3 with custom cross-fade GL compositor, JVM-tested only. Export UI now calls `NativeBridge.exportProject` (exportId-keyed) with progress + cancel; **no JS↔native export round trip has run on a real device.** |
| **M10** Local captions | **Engine done; iOS native plugin NOT wired** | Word-timestamp smoothing verified against a real whisper.cpp 1.9.2 capture; caption track + karaoke preview + EDL burn-in wired; Generate works end-to-end in the dev harness (web fallback + fixture; a silent `AddTrackCommand` bug mistyping caption tracks was found and fixed). Android whisper JNI compiles; **the iOS whisper plugin is scaffolded but not wired.** On-device transcription unverified on both platforms. |
| **M11** Performance/memory hardening | **Not started** | Never assigned in the execution campaign. Depends on real devices anyway (jetsam/OOM behavior); fold into the M1 spike follow-up. |
| **M12** QA + CI | **CI real; device QA not started** | Invariants run in CI on push; release workflow (signed APK + unsigned IPA on tag) exists but **its first real run needs repo secrets** (keystore — see RELEASING.md). Device-matrix QA blocked on hardware. |
| **M13** Direct distribution | **Drafted only** | publik listing copy + Android sideload guide + iOS build-from-source guide written under `docs/`; nothing published. Signing blocked on founder keystore/secrets. |

## The shipping gap that was closed last

Until the final pass, `apps/mobile` booted the old M3/M4 raw-JSON debug harness — the CapCut UI existed only in a web dev route. Now: `apps/mobile` boots a **project list → real `EditorShell`** (`src/app/app-root.tsx`), with the legacy harness kept behind `#/diagnostics`. Verified by: vite build, tsc, eslint, invariants green, and both native builds compiling the new bundle. **Not yet verified: a human actually tapping through the home→editor flow on a device/simulator screen.** That is deliberately listed in the runbook rather than claimed here.

## Corrections from real-device testing (2026-08-18, founder's iPhone)

- The audit's "every NativeBridge method has a real registered implementation on BOTH platforms" was **wrong for iOS**: the plugins compiled and linked but were never registered with the Capacitor bridge (no `capacitorDidLoad` registration), so every native call died with `plugin is not implemented on ios` on first real tap. Fixed via `KneecapBridgeViewController`. Lesson for future audits: *compiled ≠ registered* — plugin availability must be verified by invoking a method at runtime, not by reading `pluginMethods` lists.
- The webview CSP blocked WASM compilation (`wasm-unsafe-eval` missing) — the app hung on "Loading…" forever. Both found only because a human tapped the real app.

## Corrections from real-device testing round 2 (2026-08-19, founder's iPhone) — fixed in 6e76d486

- **Whole app page-scrolled / chrome outside the safe area.** The 2026-08-18 safe-area fix was right in intent, wrong in box model: no `box-sizing` reset loads on the real app path, so `.cc-editor-shell`'s `height: 100%` + inset paddings summed past the viewport (content-box). Same bug on `.kc-home`/`.kc-crash`. Now border-box + page scroll locked (`html/body/#app` overflow hidden, overscroll none); lists scroll internally. Lesson: env(safe-area-inset-*) fixes are invisible in any 0-inset environment — only a notched device shows the box-model mistake.
- **Playback totally dead for native imports.** M4's documented zero-byte stub `File` reached mediabunny's `BlobSource` via every decode path; each advancing frame re-parsed the empty blob and failed silently (playback clock over a black canvas — a fourth instance of the "silent death" class). Fixed by `media/playable-source.ts` (plan §2.6 swap): file-bytes-else-`UrlSource(mediaAsset.url)` in video-cache/backdrop/audio paths, plus a failed-sink negative cache + one-shot toast. **UrlSource-over-Capacitor-scheme playback has NOT yet run on a device** — verify on the next founder run.
- **Import looked dead.** Picker → sequential native transcodes with no UI until completion. `importMediaFromNative` now emits per-asset progress; EditorShell drives ProgressOverlay from tap to done.

## Corrections round 3 (2026-08-19, founder's iPhone console log) — fixed in 28e9f2f7 + e7d7a75c

The round-2 UrlSource swap surfaced the layer beneath it ("Retrying failed fetch" spam on merely opening a project):

- **iOS playback URLs were bare filesystem paths.** Swift returns raw `URL.path` values (by design — raw paths are the canonical native handles), but `Capacitor.convertFileSrc` only rewrites `file://`/`content://` strings and passes raw paths through untouched. `toPlaybackUri` now normalizes absolute paths to `file://` first. Android was never affected (`Uri.fromFile(...).toString()`). Verified in-repo that Capacitor 8.5 iOS's WebViewAssetHandler serves `_capacitor_file_` with full Range support (206 + Content-Range), so UrlSource streaming has a sound transport.
- **mediabunny's default retry policy never gives up** on same-origin fetch failures — the infinite loop kept sink-init pending, so the round-2 failed-sink cache/toast never engaged. UrlSource now caps at 3 attempts.
- **CSP listed the wrong iOS origin**: `server.hostname` applies to BOTH platforms, so iOS runs at `capacitor://appassets.androidplatform.net`, while the CSP only allowed Capacitor's default `capacitor://localhost`. Now listed in default-src and img-src.
- **Reopened projects rehydrated dead assets**: storage persisted the zero-byte stub and rebuilt `url` as an object URL of nothing; `MediaAssetData` now persists `url` for native-custody assets (and fps/hasAudio, which were declared but never round-tripped). Assets saved before this fix stay dead — recreate test projects. Known limit: the persisted URI embeds the iOS container UUID → app REINSTALLS orphan saved native assets (v1-acceptable).
- Unexplained log residue to watch next run: one boot-time "JS Eval error", repeated WebKit `AVIF initImage err=-39` decode failures (asset audit pending), and "Updated list with error: DownloadFailed" (believed iOS system noise, not app traffic — the offline gate stays green).

## Round 4 (2026-08-19): simulator-verified transport + the container-rotation fix

Round 3 still failed on device ("Load failed" on every sink init). Rather than another device round, the seam was reproduced **locally in the iOS Simulator** with a planted proxy file and a boot-time probe of the exact pipeline — and the entire transport WORKS as shipped: plain fetch returns full bytes, Range returns a correct 206, mediabunny UrlSource decodes a real first frame over the `capacitor://appassets.androidplatform.net/_capacitor_file_/...` URL (spaces in "Application Support" and all). The shipped playback code is correct for FRESH imports.

The remaining failure is environmental and now understood: **iOS rotates the app data-container UUID on every app update/reinstall** (observed live: a simctl upgrade-install rotated it too). Persisted ABSOLUTE media paths/URLs — including round 3's persisted `url` — die on the next install. Founder-device failures were rehydrated assets pointing into the previous container.

Fix (this round): container-RELATIVE persistence. New `NativeBridge.getMediaRoot()` (iOS `MediaSandbox.rootDirectory()`, Android `noBackupFilesDir`, web null), `media/native-paths.ts` resolver registry (host registers root+converter at boot, same pattern as `EdlAssetResolver`), `MediaAssetData.nativeRelativePath`/`thumbnailNativeRelativePath` persisted at import, re-anchored to the current root at every load (absolute `url` kept as same-install fallback). Assets imported before this build have no relative path and stay dead after a reinstall — recreate test projects once.

Still open, same log: the boot-time "JS Eval error", and `www/fonts/font-chunk-*.avif` failing iOS decode (err=-39, ×15 = the font atlas chunks — text glyph rendering likely degraded on iOS; needs its own investigation). NOT yet verified anywhere: an end-to-end on-device play (frames on screen) — the sim probe verified decode, not the full UI loop.

## Round 5 (2026-08-19): playback PROVEN end-to-end in the simulator — `#/autotest` harness (0dd90239, 973c5182)

The round-4 device log (with the new source instrumentation) showed the failing assets were the founder's PRE-fix clips rehydrating as `blob:`-of-zero-bytes — not the new pipeline. Instead of asking for another device round, apps/mobile gained a **`#/autotest`** route (also triggerable headlessly by planting `<mediaRoot>/autotest.flag`): it drives the real app with zero taps — real `importMediaFromNative` (only `pickMedia` substituted with a runner-planted mp4), real clip placement, real EditorShell, real play — and logs a `[autotest] VERDICT`. In-page PASS = clock advanced + videoCache decoded frames (WebKit clears WebGPU canvases after present, so in-page pixel readback is blank by design; the runner's `simctl io … screenshot` is pixel ground truth).

**All three phases verified on the iOS Simulator with screenshots showing the video actually rendering:** (1) import → plays; (2) relaunch/reopen from storage → plays; (3) **reinstall → container UUID rotated AND the data migrated (exactly like a real iOS update) → reopened project plays** — the founder-device failure scenario end to end. Storage also now: skips dead pre-persistence assets (url undefined + named console.warn, no more decoder error storm), and SALVAGES round-3 absolute-url assets by re-anchoring the stale URL's custody-relative suffix (files survive rotation; only the prefix died).

Also fixed after real research: the boot-time `AVIF err=-39` ×15 spam was the font-picker atlas chunks — AV1 High-profile yuv444 + separate monochrome alpha track (ffprobe-verified), which Chrome decodes and Apple ImageIO does not. All 15 chunks re-encoded as lossless WebP in both apps; `generate-font-sprites.ts` emits webp now.

Remaining round-5 leftovers: the boot "JS Eval error" (reproduces in sim, app unaffected — still undiagnosed); the simulator renders the editor chrome with degraded styling (huge serif fallbacks, mislaid timeline chrome — not seen in founder device captures; suspected iOS-26-sim WebKit/CSS quirk, worth one look); audio playback verified only decode-side (autotest asserts video frames, not audible output).

## Round 6 (2026-08-19): image imports died in the video transcoder — fixed + autotest now covers stills

The round-5 device log was the healthiest yet (dead legacy assets cleanly flagged, no error storms, the founder's fetch failures gone) but surfaced the next seam on first real use: **picking a JPEG fed the still to the VIDEO proxy transcoder** — AVFoundation refused with `-11828 Cannot Open … AVErrorFailedDependenciesKey=(Duration)`. Neither platform's `generateProxy` ever branched on `handle.kind`; the web fallback documented the intended contract all along ("the proxy IS the source"). NOTE for future triage: the scary LaunchServices/`usermanagerd`/sandbox-extension lines around picker use are normal out-of-process PHPicker noise, not app failures.

Fix, one layer per concern: (a) `importMediaFromNative` short-circuits `kind=="image"` — no transcode call, proxy = source, the still is its own thumbnail, relative-path persistence included; fixes iOS AND Android identically. (b) Both native `generateProxy`s now reject images loudly ("video-only") instead of streaming a cryptic AVFoundation/Media3 error if a future caller regresses. (c) Zero-length image clips fixed at placement (`duration || 3s` — a probed still has durationMicros 0, and `?? 3` never fired).

Verified in the simulator via `#/autotest` (now imports a planted mp4 AND jpeg together): both import, both land on the timeline (screenshot), video plays, VERDICT PASS on import and reopen phases (the image's `rel=Media/autotest-image.jpeg` persists + re-anchors). invariants green; both native builds compile.

## Round 7 (2026-08-19): the "stuck at import" class — pick progress + failure surfacing; fonts to PNG (1e582b4b)

Device log showed `pickMedia` fire then silence. Root cause: iOS `loadFileRepresentation` IS the iCloud original download (minutes, unobserved Progress), and per-item errors resumed nil — silently dropped, so an all-failed batch resolved `handles: []` exactly like a user cancel. Android inversely rejected the whole batch on one bad item. Fixed with a `pickProgress` event stream on both platforms (iOS KVO on the load Progress = real download fractions; Android per-item stage markers + per-item isolation), forwarded as import-stage "picking" ("Preparing media N of M…" in the overlay), with every dropped item toasted.

Fonts: lossless WebP ALSO failed device ImageIO (err=-50 ×15) — and round 5's "0 AVIF errors" sim check was vacuous because WebContent-process decode errors never reach the app console. Chunks are now PNG; the autotest VERDICT includes an in-app `Image.decode` probe (`fonts=ok`) so image-format support is measured, never assumed. Sim-verified: import phase exercises pickProgress, video+image import, playback PASS, fonts=ok; reopen PASS.

**Closed as not-a-bug:** the boot "JS Eval error" ×1-2 is Capacitor's own `CapacitorBridge.eval` firing a lifecycle event before the page loads (always pre-"WebView loaded") — benign framework noise on every Capacitor iOS app.

## Round 8 (2026-08-19): audio + timeline-follow — sim VERDICT now fully green (97486811, 53188d46)

Founder: "audio doesn't play and the timeline doesn't track." Both real, both engine-adjacent mobile-shell wiring (the OpenCut engine itself is intact — every campaign bug has lived in the new UI/native seams):

- **Every iOS proxy was silently MUTE**: nil-passthrough audio writer without a sourceFormatHint fails `writer.canAdd`, and the guard skipped audio wholesale (proven by #/autotest's new mediabunny probe: `getPrimaryAudioTrack()==null` on the proxy). Now PCM-decode → AAC 44.1k/2ch/128k encode (robust to any source codec), failed add THROWS. That exposed the classic AVAssetReader two-output stall (sequential drain + bulky PCM = 27%-in-4-minutes crawl) — track loops now drain concurrently. Plus AVAudioSession `.playback` so the ring/silent switch stops muting all WebAudio.
- **Timecode + strip froze during playback**: per-frame time rides `PlaybackManager.onUpdate`, but `useEditor` selectors ride `subscribe` (play/pause/seek only). `useCurrentTimeSeconds` now subscribes to all three channels (+ SSR getServerSnapshot for the web harness — caught after invariants printed 11/12), and TimelineView gained playback-follow (fixed-center playhead, strip scrolls beneath, no-op-set guard so syncs can't swallow the next user scrub).
- **Harness confession**: #/autotest never imported the app stylesheets (vite per-entry CSS splitting) — every "degraded UI" screenshot and the unscrollable `overflow:visible` timeline were HARNESS artifacts, not app bugs. Fixed; the route now renders and asserts the real UI. Verdict asserts audio stats (new `AudioManager.getStats`) and measured strip scroll: final sim run `PASS advanced sinks decodedFrames fonts=ok audio=ok timeline=ok(0->126px)`.

Residual: audible-through-speakers not verifiable headlessly (stats say running+scheduled); reopen-phase proxies transcoded by OLDER builds remain mute — re-import once on the new build.

## Round 9 (2026-08-19): the jetsam kill named itself + clip-selection UX (2dc1724b)

Founder's Xcode screenshot settled the crash-during-import mystery: **"killed by the operating system because it is using too much memory"** — jetsam, exactly the class the simulator can never reproduce (Mac RAM). Root cause: the proxy transcoder decoded every frame at SOURCE resolution to 32BGRA (~33MB/frame at 4K) plus per-frame CoreImage. Now `AVAssetReaderVideoCompositionOutput` — VideoToolbox delivers upright, proxy-scaled ~2MB frames appended directly, zero CoreImage. Measured (4K60 10-bit HLG import in sim): **51MB → 98MB peak footprint**, with `[kneecap-mem]` watermark logs bracketing every transcode for future triage.

Selection ("options don't change / delete not popping up"): the contextual row was one subtle "Edit" chip, Delete two taps deep. Now CapCut-style direct Split / Delete / Duplicate on the row + "Edit" for the sheet. #/autotest drives the real gesture path (dispatched pointerdown/up) end to end: select → Delete visible → press → element removed → undo restores. Note for the "did you retain OpenCut" question: yes — editor-core IS OpenCut's engine; the selection/actions all existed (M8, browser-verified for text); the gap was surfacing them per CapCut's one-tap pattern.

Full fresh-import verdict: `PASS … fonts=ok audio=ok timeline=ok(0→128px) select=ok(sel=1 delete 1→0)`. invariants 12/12 checked before push.

## Round 10 (2026-08-19): "no audio I hear" — measured signal + the AudioDecoder fallback (f43d183c)

The round-8 audio verdict was necessary-but-insufficient ("sinks opened" is not "audio flows"). Now measured: an AnalyserNode taps the master bus and #/autotest gates on non-zero RMS during playback — sim shows `sound=ok(rms=0.0895)`, i.e. real signal to the speaker driver. The device-silence candidate now closed: WebKit shipped WebCodecs **AudioDecoder years after VideoDecoder**; without it mediabunny's streaming audio sink yields nothing — no error, pure silence, all open/decode stats healthy. AudioManager now falls back to `audioContext.decodeAudioData` (native WebAudio, every iOS version) when AudioDecoder is absent, when a mediabunny decode errors, or when it comes back empty.

Founder reminders: (a) proxies transcoded by pre-AAC builds are PERMANENTLY mute — re-import clips once on the current build; (b) the "audio track visible under the clip" expectation = clip waveforms, which are still mock-only (H3) — the next visibility gap to close.

## Round 11 (2026-08-19): the audio fallback was broken twice over — caught by forcing the device profile in the sim (9e3ca374)

The founder's latest device log was the healthiest yet (real .mov import, live download progress, transcode at a FLAT 18MB footprint — the OOM fix confirmed on device) but predated the round-10 fallback. Forcing that fallback in the sim (AudioDecoder deleted = the founder-device WebKit profile) FAILED — two bugs that would each have cost a device round: (1) WebKit's `decodeAudioData` rejects movie containers, so feeding it the whole proxy could never work → the fallback now packet-copies the audio track into an audio-only mp4 via mediabunny `Conversion` (`video:{discard:true}`, no decode, no WebCodecs needed) and decodes that; (2) `readPlayableBytes` threw "HTTP 0" on Capacitor's statusless media responses (status 0/ok false with good bytes — a behavior round 4 had already documented and the guard ignored anyway).

Both paths now sim-verified with measured master-bus RMS: native WebCodecs `sound=ok(rms=0.0894)`, forced device-profile `sound=ok(rms=0.0893)`. The forced mode is a permanent opt-in harness feature (`<root>/autotest-fallback.flag`). AppDelegate keeps `.playback` but drops the eager `setActive(true)` (suspected source of the AudioComponentRegistrar "Operation not permitted" device noise — watch whether it disappears from the next log).

## Round 12 (2026-08-19): gesture-stack unlock + the sound-check bisector (c01aae94)

The AudioComponentRegistrar "Operation not permitted" line survived removing the eager `setActive` — hypothesis dead. Everything measurable in-app says signal flows; what remains is device-only, so two additions end the guessing: (1) `AudioManager.unlock()` — the canonical iOS WebAudio unlock (ensure + resume + silent one-sample buffer) called SYNCHRONOUSLY first in the play tap handler; the engine's own resume runs after async hops, the last known "silent on device only" shape. (2) A home-screen **"🔊 Test" chip** (dogfood, remove before listing): raw 440Hz tone through the editor's own context/destination + `[soundcheck]` log. One tap bisects everything left: **beep audible** → webview audio output works, the registrar line is benign, any remaining silence is pipeline-level (instrumented warnings will name it); **no beep** → webview audio output itself is broken on this device → the fix is platform-level (session/config/native routing), not pipeline-level.

## Round 13 (2026-08-19): web beep SILENT at state=running — platform verdict + native tone (e096c496)

Founder's bisect answer: **no web beep** while `[soundcheck]` logged `suspended → running`, 48kHz. WebAudio in this app's WKWebView renders silently while claiming to run — on every build to date (this is BELOW all pipeline fixes; note the AudioComponentRegistrar "Operation not permitted" WebContent errors are now presumed related, not noise). Added `NativeBridge.playTestTone()` (iOS AVAudioEngine 440Hz, Android ToneGenerator) and a second home chip: "🔊 Web" vs "🔊 Native". Decision tree, in order: **native-only beep** → (1) delete the app-level AVAudioSession config entirely — it shipped in the SAME commit as the first-ever device audio attempt, so stock-Capacitor session behavior has never been tested on this device; (2) if still silent, route preview audio natively (AVPlayer/AVAudioEngine of the proxy's audio synced to the playback clock — the CapCut-grade architecture regardless). **Neither beep** → device media volume / output route (volume-up during the tone; check Bluetooth routing). `[soundcheck]` now also logs AudioDecoder presence + userAgent, finally identifying the device's WebKit next paste.

## Round 14 (2026-08-20): NATIVE preview audio — the webview is out of the sound path on iOS (309cd2ed)

Founder bisect verdict: **native beep audible, web beep silent** → this device's WKWebView renders WebAudio silently at a "running" 48kHz context, on every build ever. Preview audio now bypasses the webview on iOS: `NativeAudioPreview.swift` (AVAudioEngine mixer, per-clip player nodes scheduled from trim offsets, live-render-clock anchored future starts, TimePitch for rate, mainMixer RMS tap), bridged as `audioStart/audioStop/audioLevel` (+ pbxproj registration), host-registered into editor-core's `native-audio-router` registry; `AudioManager` tries the native route on every play/seek and falls back to WebAudio when unavailable (web, Android — Android's webview audio expected functional, untested). v1 limits: flat per-clip volume (no gain-curve automation), full-schedule rebuild per transport action, uncorrected drift between seeks.

Sim verdict on the native route: `PASS … audio=ok(native) sound=ok(rms=0.0888)` — the mix is measured natively, not assumed. invariants 12/12, both native builds green. Founder next: pull, rebuild, reinstall, play — this is the first build where the audible path is the one proven working on the device (the native beep's own stack).

## Round 15 (2026-08-20): EXPORT works end-to-end — sim-rendered, fetched back, probe-verified (5373b7f7)

Founder's screenshot: export failing with "asset could not be resolved to a readable URL" + UI polish gaps. Architecture answer for the record: export IS OpenCut's design — EDL → native AVFoundation renderer reading ORIGINAL full-res custody files; preview-proxy quality can never affect export quality. The missing wire: the original's location was never persisted → `sourceNativeRelativePath` + `sourceRotationDegrees` now persist at import (container-relative), `buildNativeEdlAssetResolver()` feeds them to `buildEdl` (original preferred, proxy fallback for pre-fix assets), and `toEdlMediaAssets` forwards the custody fields (dropping them there starved the resolver — caught in review). Second real find: export completion AWAITED the Photos-library save, so the permission prompt hung "done" indefinitely — caught headlessly because encode+mux+integrity finished and done never came; done now emits first, Photos save is fire-and-forget.

`#/autotest` gained an EXPORT phase (real EDL, real native exporter, fetch-back with retries — `outputUri` arrives ALREADY converted; double-converting was the harness's own bug — and a mediabunny probe of the output). Final sim verdict, all EIGHT assertions: `PASS … audio=ok(native) timeline=ok select=ok export=ok(3642KB) sound=ok(rms=0.0888)`. Also: preview tap toggles playback, the sheet's actions are real buttons, sheet/scrim z-index above timeline chrome. Preview smoothness deferred to M11 explicitly (polish, not correctness, now that export independence is proven). Caveat: sim exports run at software-encode speed (~minutes); devices use hardware encoders.

## Round 16 (2026-08-20): the bottom-left-quarter export — the custom compositor never placed its frames

Founder: exported video renders only in the bottom-left fourth of the frame, rest black. Two stacked root causes, both fixed:

- **`EdlTransitionCompositor` composited raw decoded buffers with NO placement.** Custom `AVVideoCompositing` classes receive frames at coded size and AVFoundation applies neither `preferredTransform` nor any fitting — `ciContext.render(image, to: outputBuffer)` drew the source at the output buffer's bottom-left (Core Image's origin) whenever source size ≠ `renderSize`, black elsewhere. A half-size source (proxy fallback, or any canvas/source mismatch) = exactly the bottom-left quarter. Now every source goes through `SourcePlacement` — the Core Image mirror of the preview's `computeVisualTransform`: upright per `EdlAsset.rotationDegrees` (`CIImage.oriented`), contain-fit into `renderSize`, then the EDL clip transform (signed scale, rotation, center offset — the top-left/+Y-down → bottom-left/+Y-up conversion happens ONCE, in `place()`), composited over the EDL background color (flat color parsed, anything else = black). Transitions place BOTH sources before dissolving.
- **`DEFAULT_CANVAS_SIZE` is 1920×1080 landscape, so every phone project was landscape** and a portrait phone video was letterboxed by design even after the fix above. Now CapCut-style aspect adoption: the FIRST visual asset imported into an untouched factory-default project sets the canvas aspect (`getAdoptedCanvasSizeForImportedMedia`, wired at the same `addMediaAsset` seam as the fps ratchet; normalized to the 1080 class, long side capped 1920, even dims, never upscaling small sources). User choices always win (non-default size or `custom` mode → no adoption); rehydration bypasses `addMediaAsset`, so reopening old projects never mutates them.

Verified: `verify-export-pipeline` gained **step 9** — the landscape fixture exported into a portrait 540×960 canvas, asserting near-black letterbox bands top AND bottom (the old bug put the video in the bottom band) and real content across both halves of the centered band. All 9 steps pass (`top=0.00 bottom=0.00 midLeft=111 midRight=148`), steps 1–8 unchanged = no regression at identity placement. 11 new unit tests for aspect adoption (`canvas/sizes.test.ts`), editor-core suite otherwise at its pre-existing environmental baseline (48 fail on clean main in this checkout — EDL suite fixtures, predates this round). NOT yet device-verified: rotated-source uprighting (fixture is rotation 0) — needs one founder export of an iPhone-shot portrait video; the orientation mapping mirrors MediaProbe's preferredTransform decode (90→.right, 180→.down, 270→.left).

## Round 17 (2026-08-20): transitions become REAL + preview drag-to-move + the overlay export honesty guard

Founder: "transitions don't apply on the preview", "when I highlight a clip I should be able to move it around in the frame", "overlay should work — not sure if that's all built yet." Verdicts and fixes:

- **Transitions were UI-only** — the M7 sheet's own honesty note said it: confirms lived in timeline-view local state, invisible to preview AND export (the EDL producer was frozen at `transitions: []`). Now end-to-end real: `TScene.transitions` (optional field; old projects load unchanged), `timeline/transitions.ts` = a verbatim TS port of `MainTrackPlacement.swift`'s integer-tick math (same overlap-by-pull semantics, same half-shorter-neighbor-minus-one-tick clamp, same nominal→output step-function remap for overlay/audio sync; kept wasm-free like the Swift original so it unit-tests without the binary). The preview consumes it through ONE derivation seam — `timeline.getRenderTracks()` (reference-memoized for `useSyncExternalStore`) — wired into the mobile preview scene-sync, the web preview, the audio scheduler (video/audio stay in sync through compressed time), snapshots, the old web exporter, and a now-compression-aware `getTotalDuration()`. The cross-fade renders as a fade-in opacity channel injected on the incoming clip (painter's order = same result as the native CIDissolveTransition; a user-authored opacity channel is never clobbered). `buildEdl` emits `transitions[]` (fade→`cross_fade`, dormant ones dropped because the native mapper throws on them), so the SAME transition drives preview and native export. Sheet wiring: engine-backed via `setMainTrackTransition` (one undoable `TransitionsSnapshotCommand`, apply-to-all included), new "None" kind removes, sheet reopens with the existing kind/duration. v1 renders every kind as a cross-fade on BOTH sides (the native compositor's documented fallback) — kinds are stored for richer renders later. 12 new unit tests incl. the Swift harness's exact fixture numbers (0.2s window → clip-b pulled to 1.8s, total shrinks by the overlap).
- **Preview drag-to-move + pinch-scale** (`usePreviewTransformGesture` in preview-renderer.tsx): with a visual element selected, one finger drags (`transform.positionX/Y` in canvas units via the mount-width ratio), a second finger pinches to scale (sign-preserving), topology changes re-anchor so nothing jumps. Mid-gesture frames ride the engine's existing preview overlay (`previewElements`), release commits ONE undoable `TracksSnapshotCommand` via `commitPreview` — the exact mechanism the web transform handles use. Tap-to-toggle-playback preserved: gesture claims the pointer only past a 6px slop, and only a real drag swallows the up-event. NOTE: round 16's export placement applies these transforms in the exported file too — preview move == export move. NOT yet device-verified (no touch hardware here): needs a founder pass, especially pinch + the slop feel.
- **Overlays, honest status**: engine + preview fully support overlay tracks (text/sticker/graphic/video/image all render); mobile UI can add text, stickers, and the shape overlay. **Native export renders text/sticker/graphic overlays only — video/image overlay clips (PiP) silently vanish from the exported file** (`OVERLAY_KIND`/`OverlayLayerBuilder` have no media path, `CompositionBuilder` reads the main track only). Media PiP import was therefore NOT wired into "Add overlay" this round (it would manufacture the silent-death class), and the export sheet now shows a LOUD warning when overlay video/image clips exist. PiP export = its own round (extra composition lanes + compositor layers on both OS sides).

Verified: editor-core typecheck clean, mobile-ui + apps/web typecheck clean; editor-core suite 212 pass / 48 fail — the 48 are the pre-existing environmental wasm-load class, byte-identical on clean main in this checkout (verified via stash); mobile-ui 38/38. The new EDL emission test lives in the environmentally-blocked file and runs where wasm loads (CI/sim). Device follow-ups for the founder: transition feel on hardware (tap the square between clips → Fade → play across the cut), drag/pinch feel, and the known timeline-strip nuance — the strip still draws clips at NOMINAL positions, so after a transition the playhead-to-strip mapping is ahead by the transition duration (sub-second; fixing the strip to placed positions is polish, tracked).

## Round 18 (2026-08-20): direct manipulation — grab clips ON the preview, no selection ceremony

Founder clarified round 17's "overlay" ask: Duplicate already stacks a copy on a new overlay track (verified: `alwaysNew/highest` inserts at overlay[0], which paints TOPMOST in scene-builder — the stacking was already right), and the real complaint was access — round 17's selection-gated gesture read as "completely inaccessible... it should just be able to be moved from on top of the preview area."

- **Hit-test-driven grab** (`preview-hit-test.ts` + gesture rewrite in preview-renderer.tsx): pointerdown hit-tests the topmost visual element under the finger at the playhead — point-in-rotated-rect against the same contain-fit x clip-transform quad the renderer draws (mirrored math, deliberately wasm-free like timeline/transitions.ts so it unit-tests; 6 geometry tests incl. rotation and letterbox misses). Touching a clip SELECTS it and starts the drag in one motion; the selected element stays grabbable under overlapping clips; overlay[0] beats main. Runs against `getRenderTracks()` (transition-derived timing == what's on screen); the returned ref targets the nominal element commands mutate.
- **Gesture semantics**: one finger drags, second finger pinch-scales; with a selection, two fingers pinch ANYWHERE on the preview; text/caption (no cheap bounds) keep the round-17 fallback — select one, any preview drag moves it. Tap on a clip = select (swallowed); tap on empty preview = play/pause toggle, unchanged; drag never doubles as a tap.
- **Selection loop closed both ways**: TimelineView gained controlled `selectedClipId` + `onClearSelection` (fallback local state keeps the M7 harness working) — grabbing a clip on the preview highlights its strip clip; tapping empty strip clears the engine selection.

Typecheck clean (editor-core, mobile-ui); mobile-ui 44/44 (6 new); editor-core at its environmental baseline. NOT device-verified: the whole gesture feel is exactly what needs the founder's thumb — grab the duplicated PiP copy, drag it to a corner, pinch it small, tap empty space, tap play. Reminder from round 17 stands: PiP overlay clips render in preview but do NOT export yet (loud warning in the export sheet); native PiP export is its own round.

## Round 19 (2026-08-20): PiP for real — visible on duplicate, composited in the export

Founder on round 18: "preview isn't even doing pic in pic, also export NEEDS to do it." Both correct, both fixed:

- **The preview PiP was invisible, not missing.** Duplicate stacks the copy on a new overlay track at the SAME time with IDENTICAL params — a pixel-for-pixel clone rendered exactly on top of the original, i.e. it looked like nothing happened. `pipSpawnParams` (timeline/pip-spawn.ts, pure + wasm-free, 4 tests) now spawns duplicated visuals visibly: video/image at HALF their current scale plus a 6%-of-short-side nudge (flip signs preserved), other visuals nudged only. The copy is immediately seen and grabbable by round 18's direct-manipulation gesture.
- **Native PiP export is BUILT.** Overlay video clips get their own composition lanes (one per overlay track; timing through the same transition remap as every non-main track; audio rides along unless muted, volumeDb respected in the mix). Image overlay clips are decoded ONCE at build time (`loadStillImage`, EXIF-upright, capped 2160px) and carried as `.still` layers. Both flow through `OverlayVideoLayer` into `EdlTransitionCompositor`, which composites them ABOVE the main frame with the SAME `SourcePlacement` math as everything else (contain-fit x clip transform, rotation, opacity), ascending track zIndex = paint order. Instructions carry only the layers intersecting their segment; `requiredSourceTrackIDs` includes the overlay lanes. Known v1 seams, documented: CoreAnimation text/sticker/graphic overlays always composite above PiP video regardless of authored z; a PiP clip extending past the main track's end is cut at the main-track duration.
- **Export sheet warning removed** — nothing left in the media class to warn about.

Verified: `verify-export-pipeline` step 10 — a 0.35-scale overlay clip (offset source time) placed into the top letterbox band that step 9 proves is otherwise black: `pipRegion(active)=129.96`, outside-quad corner `0.00`, after-window `0.00`, main band beneath `129.46`; all 10 steps pass, 1–9 unchanged. TS: typecheck clean, mobile-ui 44/44, editor-core 216 pass at the unchanged environmental baseline (48). Founder device pass: duplicate a clip → the copy appears half-size and nudged → drag/pinch it → export → the PiP is in the file.

## Round 19.1 (2026-08-20): the gesture RUN LIVE for the first time — two hit-test bugs found and fixed in the browser harness

Founder: "still can't fuck around with video above the timeline." Root context first: **rounds 16-19 are commits on this Mac that have never reached the phone** — any device test without `bun run build && bunx cap sync` + a rebuild is running the round-15 bundle, which has NO preview gestures at all. But instead of blaming the build, the gesture was executed for the first time ever, live, in the web harness (`/dev/mobile-editor`, real EditorCore, Chrome-driven pointer events). It mostly worked — drag moved a clip by the exact delta, selected-priority grabbed the selected clip through the one stacked on top, taps were swallowed correctly — and it surfaced two REAL bugs, both fixed and re-verified live:

- **Dimension-less elements were untouchable**: `sourceSizeFor` returned null for a video/image whose asset record lacks probe dims or a sticker without intrinsic dims → the hit-test skipped them → touching them fell through to play/pause. Now they fall back to a full-frame quad (grabbability guaranteed, bounds approximate).
- **...and the first version of that fallback stole every touch**: the demo's dimension-less sticker sat topmost and its full-frame quad claimed the entire preview, shadowing the precisely-bounded shape beneath (verified live: every touch logged `hit: sticker`). Hit-testing is now TWO passes — precise quads first, fallback quads only when nothing claimed the touch (`hit: graphic:Overlay shape` after the fix, and the shape visibly moved).
- Plus a defensive `try/catch` around `setPointerCapture` (invalid pointerId must not kill a drag).

mobile-ui 45/45 (2 new hit-test cases). The founder's device loop, IN ORDER, from ~/kneecap: `bun run build && bunx cap sync` → Xcode rebuild/reinstall → import video → touch it in the preview.

## Round 20 (2026-08-20): captions transcribe REAL clips — Apple Speech on iOS, whisper stays Android

Founder: "add transcription for captions, look into how CapCut actually does it, worried whisper will be too heavy for mobile." The research, then the build:

**Research verdict.** CapCut's auto-captions are SERVER-side — audio uploads to ByteDance's ASR (they fail offline); there is nothing on-device to copy there. whisper.cpp on iOS is viable but genuinely heavy: 32–60MB model (tiny/base q5) bundled or downloaded, a few hundred MB peak RAM, an out-of-repo xcframework build, hand-wired pbxproj embedding, and the audio-decode pipeline `WhisperTranscriber.swift`'s own header lists as unimplemented. Apple's Speech framework costs ZERO app bytes, runs on-device (private, offline), and emits per-word timestamps + confidence — exactly the wire contract's coarse envelope, with `caption-smoothing.ts` running unchanged on its words (`dtwStartMicros` null == "no refinement signal", already handled). **Decision: Apple Speech is the iOS engine; whisper.cpp remains the Android engine (already built there) and a documented iOS quality option; iOS 26's `SpeechAnalyzer`/`SpeechTranscriber` (the plan's own "preferred path" note) is the designated upgrade once this baseline is device-proven.** Local-first enforced: `requiresOnDeviceRecognition` always — a locale with no on-device model errors with instructions instead of silently uploading.

**Built (M10's missing iOS half + the missing panel affordance):**
- `NativeMedia/AppleSpeechTranscriber.swift` (pure Foundation+Speech: recognition + wire mapping, harness-compilable) + `NativeBridgePlugin+Transcribe.swift` (Capacitor glue; PERMISSION_DENIED / UNSUPPORTED / IO_ERROR code mapping) + `transcribe` CAPPluginMethod + `NSSpeechRecognitionUsageDescription` + pbxproj registration (round-14 NativeAudioPreview precedent).
- TS bridge un-masking: the old catch collapsed EVERY native failure to NOT_IMPLEMENTED; now only Capacitor's own UNIMPLEMENTED (an old native build) does — real errors (permission, locale, IO) propagate with their codes.
- The panel finally transcribes REAL clips: `generateCaptionsForClip` (selected video clip, else first main-track clip → custody path original-preferred like the export resolver → bridge → `windowSegmentsToClip` trims the transcript to the clip's [trimStart, trimStart+duration) and shifts it clip-relative → captions land under the audible audio) with the dev-fixture path demoted to a web-harness fallback. `windowSegmentsToClip` extracted pure (4 tests).

**Verified:** new `verify-transcription` harness (same pattern as verify-export-pipeline): 10 pure wire-mapper checks AND the LIVE half — `say`-generated speech recognized ON-DEVICE on this Mac through the exact shipped Swift file: real transcript containing the expected words, word tokens monotonic, integer-micros envelopes. (First run exposed a real harness deadlock: Speech delivers callbacks on the main queue — the harness now pumps the run loop; the app is unaffected, Capacitor calls resolve off-main.) mobile-ui 50 tests green (4 new windowing); native-bridge at its 1-fail environmental baseline (verified identical on clean main); bundle rebuilt + synced. Founder device pass: import a clip with speech → Captions → Generate → grant the speech permission prompt → karaoke captions under the clip; then Export (captions burn in via the existing EDL path).

## Round 21 (2026-08-20): the sheet-clipping bug + text content editing + swipe-to-delete — all verified live in the real app

Founder: "i dont see a generation button here for captions", "there should be a slide to delete projects on home", "text isnt editable when i add text." All three were real:

- **Panels CLIPPED their content with no scroll** — `.cc-sheet` had `max-height: 70vh; overflow: hidden`, so anything below the fold in ANY panel was silently cut off; on a phone the Captions panel's Generate button was simply not on screen. Sheets scroll now (`overflow-y: auto` + touch scrolling), AND Generate moved above the Language/style rows so it's the first thing in the panel.
- **Text had styles but no words** — the Text panel offered font/size/color/align/bold/italic and no way to edit `params.content`. A live-bound textarea now sits at the top of the panel (same undoable UpdateElementsCommand path as the style rows). Verified live in the REAL app (vite shell, real project): typed into the field and the preview rendered "kneecap rules" keystroke-by-keystroke.
- **Swipe-to-delete on home** — CapCut-style: swipe a project row left to reveal Delete (pointer events, `touch-action: pan-y` so the list still scrolls), tap Delete → confirm → the engine's real `deleteProjects` (project + its media custody, permanent). Two bugs caught during the live pass: the trailing click after a swipe instantly re-closed the row (one-shot click suppressor), and Delete showed through the transparent row at rest (now mounted only while swiped). Verified live: swipe opens at -88px, Delete removes the project from the list.

mobile-ui 50 tests green, all typechecks clean, bundle rebuilt + synced to ios/App/App/public. Founder loop: Xcode ⌘R, then (1) Captions → Generate is the top button, (2) Text → Add text → type in the Text field, (3) home → swipe a project left → Delete.

## Round 21.1 (2026-08-20): the empty-transcript device bug — video containers need audio extraction

Founder's device payload: one segment, ONE empty zero-length token, for a clip full of spoken words. Root cause: `SFSpeechURLRecognitionRequest` fed a VIDEO container (.mov/.mp4) "succeeds" while reading no audio at all — the round-20 harness passed because it fed a pure audio file (the exact class of gap the harness's honest-scope note warned about). Three-part fix:

- **Audio extraction first**: `extractAudio` (AVAssetExportSession → temp .m4a, any container, no-audio-track clips error clearly) now runs before recognition; the temp file is deleted after. Harness step 3 exercises extraction against the actual mp4 VIDEO fixture (1 audio track out, real 4.00s duration) and the live-recognition path now runs THROUGH extraction end-to-end (aiff → m4a → on-device recognition, still green with monotonic word envelopes).
- **Empty tokens are noise, never words**: `wireResult` filters whitespace-only tokens — the phantom "" word can never ship again; a genuinely speechless clip returns `segments: []`.
- **Silence is now loud**: a zero-caption result THROWS "No speech was detected in this clip's audio..." instead of the panel reporting done with nothing inserted.

All harness checks pass; mobile-ui 50 green; bundle rebuilt + synced. Founder: Xcode ⌘R, retry Generate on the same clip.

## Round 21.2 (2026-08-20): the recognizer lifetime bug + a diagnostic trail for the next device report

Founder: still "no speech detected" (with an unrelated-looking `WebContent ... sandbox extension` log — that's the WEBVIEW process; the transcriber runs native and never touches WebContent). Auditing the native path found a REAL classic: `SFSpeechRecognizer` was a local nothing retained once `recognize()` returned — deallocating mid-task kills recognition quietly, which can surface as exactly an empty "final" result; the Mac harness passed on timing luck. The task's closure now retains the recognizer explicitly (released on both completion paths). Plus a stage-by-stage `[kneecap-stt]` console trail: extracted-file bytes, recognizer locale/availability/on-device flags, raw word count — so if the device still yields zero words, the next report pinpoints the failing stage instead of guessing. Harness re-verified end-to-end (extraction 11KB m4a → locale=en-US onDevice=true → 9 raw words → full transcript). Founder: Xcode ⌘R (Swift changed; no bundle change needed), retry Generate, and if it still fails paste the three `[kneecap-stt]` lines.

## Test sweep (2026-08-18, Fable fork agent) — see docs/TEST-REPORT.md

All three CRITICALs found by the sweep are fixed and re-verified live in the browser harness: (C1) GPU init now gates the preview renderer (plus boot-time `ensurePreviewGpu` + font atlas bundled into apps/mobile, so text renders with real fonts); (C2) the home project list re-renders via a selector subscription — the engine always had the data, the UI never re-subscribed (verified: engine 1 / DOM 0 before, 1/1 after; reopen-after-reload rehydrates); (C3) a CrashBoundary paints any React render crash on screen instead of silent black. HIGHs still open, in priority order: Android's EDL parser missing ~12 field families that TS emits and iOS parses (must parse-or-reject, never silently drop); split-at-boundary silently no-ops; audio waveforms never populated (mock-only).

## Known residual defects / debt

- Trim-commit wiring in the timeline (disclosed in `timeline-view.tsx`).
- `demo-project.ts` bootstrap leaves an element pre-selected (noted by fixer; dev-harness-only).
- Whisper model is fetched at build time, not committed; CI needs network for that step (documented known gap in the offline audit for the web ML runtime signatures).
- 3 pre-existing unit-test failures in `masks/snap.test.ts` (documented baseline; predates this campaign).
- Spike tests 1/6 can only run in a foregrounded tab/app (rAF-gated) — fine on real hardware.
