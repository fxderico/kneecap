<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- Launch theme for BOTH activities (see AndroidManifest.xml). Parented
         on the AppCompat NoActionBar theme, NOT `Theme.SplashScreen`: both
         FirstRunActivity and MainActivity (Capacitor's BridgeActivity) are
         AppCompatActivity subclasses, and AppCompat throws
         "You need to use a Theme.AppCompat theme (or descendant)" from
         setContentView when the activity theme isn't one. `Theme.SplashScreen`
         only becomes AppCompat after `installSplashScreen()` swaps in
         `postSplashScreenTheme`, which neither activity calls — so the app
         hard-crashed on every cold launch (caught on the first emulator boot).
         The splash drawable is the window background either way. -->
    <style name="AppTheme.NoActionBarLaunch" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>
</resources>